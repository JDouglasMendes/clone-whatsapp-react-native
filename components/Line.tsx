import React from "react";
import { View } from "react-native";

const Line: React.FC = () => {
  return (
    <View
      style={{
        borderBottomColor: "gray",
        borderBottomWidth: 0.8,
        marginTop: 5,
      }}
    />
  );
};

export default Line;
